# T11.16 — Small wins, docs, help, final review pass

**Size:** M · **Depends on:** T11.15, T10.12 · Design §14 · Atlas tab 19

## Deliverables
- `SubmoduleNotice` → `SubmoduleCard`: recorded vs checked-out, URL, `in sync`/`drifted`, `Open
  submodule as its own repository` (opens the nested folder handle: `getDirectoryHandle(path)` read
  mode, follows a `gitdir:` file that resolves inside the picked folder).
- `Worktrees` popover from the palette / repo name tooltip: list with branch, `this folder`,
  `prunable`.
- LFS card (pointer oid, sizes, "object not fetched"), notes on CommitCard (T11.5 already renders
  `note` if present — verify), jj tag next to the repo name and no detached-HEAD banner when `jj`.
- Docs: README feature list and limitations rows; `docs/privacy.md` (exports, snapshot, `'self'`);
  `docs/errors.md` complete; `START.md` board rows for T10.x/T11.x; HelpDialog final key list;
  `docs/perf.md` v2 numbers from `scripts/perf.ts` (history first page, blame, insights walk).
- Final pass: run `bun run check`, `bun run e2e`, `bun run perf`; fix anything red; knip-style unused
  export sweep of new modules.

## Tests
- Submodule card states, worktree popover, LFS card, jj tag; docs tests (`errors.docs.test.ts`) cover
  the new codes.

## Acceptance
- [ ] Everything green; CHANGELOG entry lists owner steps left.
