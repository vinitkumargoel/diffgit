# T11.7 — Branches mode and tags

**Size:** M · **Depends on:** T11.6, T10.5 · Design §14.6 · Atlas tab 09

## Deliverables
- `BranchesView`: header row (`Active | Stale > 90 d | Tags`, search), table per §14.6 with lazily
  filled cells (`branchCells` for visible rows, 50 at a time), ahead/behind bars, `merged` / `pull` /
  `push` tags, badges `HEAD` / `default` / `remote`; sort by age; keyboard row navigation.
- Row `…` menu: `Set as compare`, `Set as base`, `Compare with base`, `Preflight rebase onto base`
  (T11.13 wires; hidden until then), `Copy name`.
- Tags table: name, annotated/lightweight, message (first line), tagger, age, `Compare with previous
  tag` (range `prev…this`).
- Mock recordings from `history`/`tags`.

## Tests
- Table renders, cells fill lazily, filters, tags table, row menu actions set sources; a11y (`grid`).

## Acceptance
- [ ] `bun run check` green.
