# T11.12 — Insights mode

**Size:** M · **Depends on:** T11.11, T10.9 · Design §14.6 · Atlas tab 13

## Deliverables
- `InsightsView`: header `90 d | 1 y | All` (pref `insightsPeriod`), sections Hotspots (horizontal
  bars, `--accent`, manifests `--grey`-toned and unranked, click → file filter in Files mode),
  Activity (52 × 7 heatmap, `--heat1..5` quintiles, tooltips with date and count, weekday labels),
  Contributors (collapsed `<details>` "Show contributors", commit counts only, bots count line,
  `.mailmap` note); `walked`/`capped` footer; loading skeleton with progress; cached in `derived`
  by tip oid + period.

## Tests
- Sections render from a recording, period switch re-requests, cache hit, contributors collapsed by
  default, hotspot click sets the filter and mode; a11y (heatmap cells have labels).

## Acceptance
- [ ] `bun run check` green.
