# T11.1 — Shell: store fields, prefs, derived cache, ModeSwitch, command palette, help

**Size:** M · **Depends on:** T10.1 (codes) · Design §14.1, §14.7

## Deliverables
- Store/prefs/persistence additions from `docs/v2-contracts.md` § Store and § Persistence
  (`derived.ts` with `getDerived/setDerived/clearDerived/derivedSize`, guarded).
- `src/ui/warnings.ts` + `docs/errors.md`: copy for every new code (level: `SECRETS_FOUND` error,
  `OPERATION_IN_PROGRESS` info/warning, `SNAPSHOT_MODE` attention, `JJ_COLOCATED` info, others warning).
- `ModeSwitch.tsx` in TopBar row 1 after the repo name (segmented, `Files | History | Branches |
  Insights`, `aria-label="View"`, keys `1`–`4`); `RepoScreen` renders `<FilesView>` (today's body),
  and placeholders `<HistoryView>`, `<BranchesView>`, `<InsightsView>` (an `EmptyState` "coming in
  T11.x" is acceptable in this task only).
- `CommandPalette.tsx` (cmdk, `⌘K`/`Ctrl+K`, icon button before the theme toggle): sections
  `Actions` (open repository, refresh, force refresh, switch mode ×4, toggle split, toggle whitespace,
  toggle hidden files, show shortcuts, clear derived cache) and `Go to file` (visible files, fuzzy).
  Search scopes are added by T11.8. Escape closes; focus returns to the trigger.
- HelpDialog: new keys section (`1`–`4`, `⌘K`, and placeholders for `b h g x e Shift+H` marked "when
  available"), footer "Cached data: <size> · Clear".
- `useShortcuts`: the new keys with the same guard rules as today (not while typing in inputs).

## Tests (vitest)
- prefs validation for every new key; ModeSwitch renders, keys switch mode, `aria-pressed`; palette
  opens/closes, lists actions, runs one (mode switch), keyboard navigation; `derived.ts` round-trip with
  fake-indexeddb and the storage-error path; a11y test for palette and ModeSwitch; snapshot of TopBar
  updated deliberately.

## Acceptance
- [ ] Repo screen in Files mode is pixel-identical to v1 except the ModeSwitch and palette button.
- [ ] `bun run check` green.
