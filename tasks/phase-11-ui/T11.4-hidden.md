# T11.4 — Why is this file hidden

**Size:** S · **Depends on:** T11.3, T10.4 · Design §14.3, §14.4 · Atlas tab 08

## Deliverables
- Sidebar row-1 toggle `lucide:eye-off` (pref `showHidden`, key `Shift+H`) → `Hidden` group appended
  to the grouped tree (also in Path mode), rows muted with a `ref`-style tag per `HiddenEntry.kind`,
  ignored directories as one row with the count; group header count.
- `WhyHiddenPopover`: on row click, `explainPath` → reasons list with the copyable command; also
  reachable for any visible file from the card header `…` (T11.6 adds the menu; here a right-click /
  `?` key on a focused row opens it).
- Settings in HelpDialog: `Apply built-in excludes` (pref `builtinExcludes`, passed to `open()`;
  changing it re-opens the session), closing backlog B10.

## Tests
- Toggle adds/removes the group; entries render by kind; popover content for each reason kind on
  recorded `hidden` data; pref persists; a11y.

## Acceptance
- [ ] `bun run check` green.
