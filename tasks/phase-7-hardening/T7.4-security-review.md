# T7.4 — Security & privacy review

**Phase:** 7 · **Size:** S · **Depends on:** T7.1 · **Blocks:** T7.5
**Plan refs:** §10, D15, D16

## Checklist
- [ ] Production headers at diffgit.com match `public/_headers`; CSP has `connect-src 'none'`; verify with `curl -sI`, the T7.1 spec 3 zero-network assertion, and one manual DevTools "Network" check on a real repo.
- [ ] `showDirectoryPicker` always called with `mode: "read"`; grep guard for `readwrite` in `src/**`.
- [ ] `FsaFs` write-throw invariant tests present and passing; the spy-based zero-write assertion runs in the session integration test (T3.5).
- [ ] Rendering of repo content is inert: file text goes through the diff renderer as text nodes (no `dangerouslySetInnerHTML` with repo content; Shiki output is trusted markup from our own tokeniser, not from the file), SVG images rendered via `<img src=blob:>` only, filenames rendered as text.
- [ ] No `eval`/`new Function`; `wasm-unsafe-eval` only if the renderer needs it (note which package).
- [ ] IndexedDB stores only handles, names, branch names, viewed keys — no file content.
- [ ] Run the `security-reviewer` agent on `src/ui/**` and `src/engine/fs/**`; address findings.
- [ ] `docs/privacy.md` written (what is stored, what is never sent, how to clear: "Clear site data").

## Acceptance criteria
- [ ] All boxes ticked with evidence in CHANGELOG.

## Amendments (review round 1)
- [ ] `scripts/check-prod.sh` (T0.2) passes: no Cloudflare-injected scripts (Web Analytics, Rocket Loader, Email Obfuscation, Zaraz) in the served HTML.
- [ ] D16 lint guard from T0.4 is present in `bun run check` and passes.
- [ ] Zero-write proof by mtime on a real repo (`find .git -newer marker`) recorded (from T1.6 script).
