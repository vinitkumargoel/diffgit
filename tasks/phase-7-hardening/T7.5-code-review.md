# T7.5 — Code review and simplification passes

**Phase:** 7 · **Size:** M · **Depends on:** T7.2, T7.3, T7.4 · **Blocks:** T8.1

## Deliverables
- Run `code-reviewer`, `typescript-reviewer`, `react-reviewer` agents over `src/engine/**` and `src/ui/**`; triage findings into fix-now vs backlog (`docs/backlog.md`).
- Run `code-simplifier` / `refactor-cleaner` (knip for dead exports) — remove unused code, collapse duplicated helpers (path normalisation, error mapping), ensure `engine/` public surface is only what `session.ts` exports.
- Confirm layering guards (`bun run check` greps: no React in engine, no `instanceof FileSystem*`, no direct worker calls from components, no `readwrite`, no empty catches) are all present and documented in `docs/contributing.md`.

## Acceptance criteria
- [ ] Reviews recorded; all fix-now items merged; `bun run check` and E2E green after refactors.
