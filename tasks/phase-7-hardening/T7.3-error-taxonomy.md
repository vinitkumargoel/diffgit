# T7.3 — Error taxonomy & silent-failure review

**Phase:** 7 · **Size:** M · **Depends on:** T7.1 · **Blocks:** T7.5
**Plan refs:** README conventions (errors), §5.4 retries, §11 R2

## Deliverables
- `docs/errors.md`: table of every `EngineError` code → where thrown → user copy → user action. Generated partly by grep; kept in sync by a test that asserts the set of codes in `src/engine/errors.ts` equals the documented set and the `describeError` map (T4.1).
- Review every `catch` in `src/**` (run the `silent-failure-hunter` agent): each must rethrow, convert to a typed error, or emit a warning code — no bare swallow. `CANCELLED` may only be swallowed in the scheduler (T6.1).
- Torn-read handling verified: `readIndex` retry path has a test using `memoryDirHandle` that swaps bytes between reads; ref resolution during a lock window (`ENOENT` on `refs/heads/x` while `packed-refs` has it) handled by isomorphic-git — add a test.
- Worker crash recovery: if the worker throws outside a call (`onerror`), the client re-creates it and the store shows "Engine restarted, refreshing".

## Acceptance criteria
- [ ] Test enforcing code/doc/copy parity green; review notes in CHANGELOG; no `catch {}` or `catch (e) {}` bodies in `src/**` (grep guard in `bun run check`).

## Amendments (review round 1)
- The registry is `src/engine/errors.ts` (T0.1); this task verifies parity between `PUBLIC_CODES`/`WARNING_CODES`, `docs/errors.md`, `describeError`, and `WarningBanners` copy.
- Add `STALE` and `WORKER_CRASHED` flows to the review: stale responses must be dropped silently, worker crashes must restart exactly once per crash.
