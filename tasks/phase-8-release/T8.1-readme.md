# T8.1 — README and docs

**Phase:** 8 · **Size:** S · **Depends on:** T7.5 · **Blocks:** T8.2

## Deliverables
`README.md` sections: what it is (one paragraph + screenshot light/dark), how to use (3 steps), privacy model (from `docs/privacy.md`), supported browsers (Chromium desktop) and why, **limitations** (verbatim list): racy-git same-ms edits until force refresh; no mode-change detection from worktree; no autocrlf normalisation; global excludes not applied; split index unsupported; sparse index partial; worktrees (`gitdir:`) and reftable unsupported; no copy detection; LFS pointers shown raw; symlink targets by content only; limits table from Plan §6.7. Development section (`bun run dev`, fixtures, tests, deploy). Link to Plan.md and docs/.
`docs/` index linking adr-001, engine-notes, errors, perf, privacy, deploy, contributing.

## Acceptance criteria
- [ ] README reviewed for accuracy against the shipped behaviour (every limitation has a test or a note).

## Amendments (review round 1)
Limitations list updates: racy-git edits are now detected via git's own rule (remove from limitations; keep force refresh as belt-and-braces); symlinks and submodules are compared only at the committed/staged level (worktree edits to them are not detected); `.DS_Store`-style built-in excludes are applied by default (toggle in settings); SHA-256 repos, alternates, partial clones, reftable, split index are refused with an explanation; packs above 1 GB are refused.
