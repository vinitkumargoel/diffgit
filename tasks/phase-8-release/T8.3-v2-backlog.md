# T8.3 — v2 backlog

**Phase:** 8 · **Size:** S · **Depends on:** T8.2

## Deliverables
`docs/backlog.md` with sized entries, each referencing the abstraction that makes it possible:
- `DiffSource` kinds: `{kind:"commit", oid}` (single commit via `git show` semantics) and `{kind:"range", from, to}` (arbitrary refs/SHAs); UI input for SHAs.
- Commit history panel (`readCommit` walk from source with pagination) and picking a commit range from it.
- Per-path incremental recompute (scheduler passes `paths`; engine re-scans only those and patches `DiffResult`).
- Copy detection (`-C`).
- autocrlf / `.gitattributes` `text`/`eol` normalisation.
- LFS pointer rendering ("LFS object, N bytes").
- Multi-pack-index and reftable support (depends on isomorphic-git upstream or own reader).
- Firefox/Safari fallback investigation (`webkitdirectory` upload mode for small repos).
- Sharable read-only snapshot export (static HTML of a diff) — still no server.
- Items triaged from T7.5 reviews.

## Acceptance criteria
- [ ] File exists, each item sized S/M/L with a one-line rationale.
