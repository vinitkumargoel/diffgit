# T8.2 — Deploy v1

**Phase:** 8 · **Size:** S · **Depends on:** T8.1 · **Blocks:** T8.3
**Plan refs:** §13 owner sign-off checklist

## Steps
1. `bun run check && bunx playwright test` green on `main`.
2. `bun run deploy`; verify https://diffgit.com serves the new build (check a build-id string rendered in the help dialog).
3. Owner smoke test on three real repos: walk the §13 checklist; confirm `git status` output identical before/after each session; confirm DevTools Network empty after load.
4. Tag `v1.0.0`; GitHub release notes from CHANGELOG.
5. Optional: connect Pages to the GitHub repo for automatic deploys on `main` (document in `docs/deploy.md`).

## Acceptance criteria
- [ ] All §13 boxes ticked by the owner; tag pushed.

## Amendments (review round 1)
- Step 3 adds: run `scripts/check-prod.sh` (no injected scripts, CSP present) and the mtime zero-write proof (`touch marker; <session>; find .git -newer marker -type f` prints nothing) on each of the three repos.
