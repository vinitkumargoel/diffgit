# Fixture repositories

Generated by `bun run fixtures` (`scripts/make-fixtures.sh` + `scripts/fixture-expectations.sh`).
Never edit by hand; rerun the script. Identity `test <test@example.com>`, dates `2024-01-01T00:00:00Z`,
so every oid is deterministic. `_remotes/` holds the local bare repos used as "origin";
`_worktree-main/` is the main repo behind `worktree-gitdir`. Each fixture has an `expected/`
directory with JSON dumped from git (see the expectations script for the exact commands).

| Fixture | HEAD | Branches | What it exercises | Verify |
|---|---|---|---|---|
| `basic` | `feature` | `main`, `feature` | 3 commits on main, 2 on feature, main moves on after divergence (two-dot ≠ three-dot) | `git log --oneline --graph --all` |
| `renames` | `feature` | `main`, `feature` | exact rename, ~60 % rename, below-threshold rewrite, identical content moved between dirs | `git diff -M --name-status main...feature` |
| `binary` | `feature` | `main`, `feature` | PNG modified/added, `.bin` with NULs, SVG (text), text file that gains a NUL | `git diff --numstat main...feature` |
| `worktree` | `feature` | `main`, `feature` | staged / unstaged / both / staged-new / untracked (+ new dir) / deleted-unstaged / deleted-staged / `chmod +x` / reverted-to-base | `git status --porcelain=v2 --untracked-files=all` |
| `conflict` | `main` (merging) | `main`, `feature` | mid-merge, `file.txt` has stages 1/2/3 | `git ls-files -s` |
| `packed` | `feature` | as `basic` | `gc --aggressive` + `pack-refs --all`: no loose refs, delta pack | `ls .git/refs/heads; cat .git/packed-refs` |
| `remote` | `feature` | `main`, `feature`, `topic`, `origin/*` | clone of `_remotes/basic.git`; `origin/HEAD` → `origin/main` | `git symbolic-ref refs/remotes/origin/HEAD` |
| `remote-master` | `feature` | `master`, `feature`, `origin/*` | default branch named `master`, no `main` | same |
| `no-origin-head` | `main` | `main`, `master`, `feature` | `origin/HEAD` removed; local `main` must beat `master` (D8) | `git symbolic-ref refs/remotes/origin/HEAD` (fails) |
| `index-v4` | `feature` | as `worktree` history | index format 4 with staged changes | `git update-index --show-index-version` |
| `index-v3` | `feature` | as `worktree` history | index format 3, `git add -N intent.txt` (extended flags) | same |
| `ignore` | `main` | `main` | nested `.gitignore` with negation, `info/exclude`, 200-file `node_modules/`, tracked file inside an ignored dir | `git check-ignore -v --no-index <paths>` |
| `detached` | detached at `feature~1` | `main`, `feature` | detached HEAD | `cat .git/HEAD` |
| `unborn` | `main` (unborn) | — | `git init` + staged files, no commits | `git status` |
| `unrelated` | `feature` | `main`, `feature` | orphan branch, no merge base | `git merge-base main feature` (fails) |
| `crlf` | `feature` | `main`, `feature` | CRLF file committed verbatim; `core.autocrlf=true` in config | `git config core.autocrlf` |
| `large` | `feature` | `main`, `feature` | 3000 changed lines, 1.5 MB text modified, 11 MB text added | `git diff --stat main...feature` |
| `embedded` | `feature` | as `basic` | nested repo `inner/.git`, untracked | `git status --porcelain=v2` |
| `symlink` | `feature` | `main`, `feature` | `link` retargeted (120000), `link2` symlink → regular file (typechange), target edited | `git diff --name-status main...feature` |
| `worktree-gitdir` | `feature` | — | `.git` is a file with `gitdir:` (linked worktree of `_worktree-main`) | `cat .git` |
| `submodule` | `feature` | `main`, `feature` | gitlink bumped on feature; submodule deinitialised (empty dir) | `git ls-files -s sub` |
| `crisscross` | `feature` | `main`, `feature` | two merge bases | `git merge-base --all main feature` |
| `rebase-detached` | detached (rebase) | `main`, `feature` | interrupted `git rebase -i` (`.git/rebase-merge/` present) | `git status` |
| `attributes` | `feature` | `main`, `feature` | `.gitattributes`: `-diff`, `binary`, `linguist-generated`, `text` | `git diff --numstat main...feature` |
| `sha256` | `main` | `main` | `--object-format=sha256` (refused by the engine) | `git rev-parse --show-object-format` |
| `alternates` | `feature` | as `basic` | `.git/objects/info/alternates` (refused) | `cat .git/objects/info/alternates` |
| `partial` | `main` | `main`, `origin/*` | `--filter=blob:none` promisor clone (refused) | `git config remote.origin.promisor` |
| `perf-5k` | `feature` | `main`, `feature`, `other` | 5,000 files / 200 dirs, packed; only with `FIXTURES_PERF=1` | `git ls-files \| wc -l` |

`expected/` in every fixture is listed in that fixture's `.git/info/exclude` (written by the expectations script) so neither git nor the engine reports it as untracked.
