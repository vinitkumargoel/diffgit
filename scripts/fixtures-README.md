# Fixture repositories

Generated by `bun run fixtures` (`scripts/make-fixtures.sh` + `scripts/fixture-expectations.sh`).
Never edit by hand; rerun the script. Identity `test <test@example.com>`, dates `2024-01-01T00:00:00Z`,
so every oid is deterministic. `_remotes/` holds the local bare repos used as "origin";
`_worktree-main/` is the main repo behind `worktree-gitdir`. Each fixture has an `expected/`
directory with JSON dumped from git (see the expectations script for the exact commands).

| Fixture | HEAD | Branches | What it exercises | Verify |
|---|---|---|---|---|
| `basic` | `feature` | `main`, `feature` | 3 commits on main, 2 on feature, main moves on after divergence (two-dot ≠ three-dot) | `git log --oneline --graph --all` |
| `renames` | `feature` | `main`, `feature` | exact rename, ~60 % rename, below-threshold rewrite, identical content moved between dirs | `git diff -M --name-status main...feature` |
| `binary` | `feature` | `main`, `feature` | PNG modified/added, `.bin` with NULs, SVG (text), text file that gains a NUL | `git diff --numstat main...feature` |
| `worktree` | `feature` | `main`, `feature` | staged / unstaged / both / staged-new / untracked (+ new dir) / deleted-unstaged / deleted-staged / `chmod +x` / reverted-to-base | `git status --porcelain=v2 --untracked-files=all` |
| `conflict` | `main` (merging) | `main`, `feature` | mid-merge, `file.txt` has stages 1/2/3 | `git ls-files -s` |
| `packed` | `feature` | as `basic` | `gc --aggressive` + `pack-refs --all`: no loose refs, delta pack | `ls .git/refs/heads; cat .git/packed-refs` |
| `remote` | `feature` | `main`, `feature`, `topic`, `origin/*` | clone of `_remotes/basic.git`; `origin/HEAD` → `origin/main` | `git symbolic-ref refs/remotes/origin/HEAD` |
| `remote-master` | `feature` | `master`, `feature`, `origin/*` | default branch named `master`, no `main` | same |
| `no-origin-head` | `main` | `main`, `master`, `feature` | `origin/HEAD` removed; local `main` must beat `master` (D8) | `git symbolic-ref refs/remotes/origin/HEAD` (fails) |
| `index-v4` | `feature` | as `worktree` history | index format 4 with staged changes | `git update-index --show-index-version` |
| `index-v3` | `feature` | as `worktree` history | index format 3, `git add -N intent.txt` (extended flags) | same |
| `ignore` | `main` | `main` | nested `.gitignore` with negation, `info/exclude`, 200-file `node_modules/`, tracked file inside an ignored dir | `git check-ignore -v --no-index <paths>` |
| `detached` | detached at `feature~1` | `main`, `feature` | detached HEAD | `cat .git/HEAD` |
| `unborn` | `main` (unborn) | — | `git init` + staged files, no commits | `git status` |
| `unrelated` | `feature` | `main`, `feature` | orphan branch, no merge base | `git merge-base main feature` (fails) |
| `crlf` | `feature` | `main`, `feature` | CRLF file committed verbatim; `core.autocrlf=true` in config | `git config core.autocrlf` |
| `large` | `feature` | `main`, `feature` | 3000 changed lines, 1.5 MB text modified, 11 MB text added | `git diff --stat main...feature` |
| `embedded` | `feature` | as `basic` | nested repo `inner/.git`, untracked | `git status --porcelain=v2` |
| `symlink` | `feature` | `main`, `feature` | `link` retargeted (120000), `link2` symlink → regular file (typechange), target edited | `git diff --name-status main...feature` |
| `worktree-gitdir` | `feature` | — | `.git` is a file with `gitdir:` (linked worktree of `_worktree-main`) | `cat .git` |
| `submodule` | `feature` | `main`, `feature` | gitlink bumped on feature; submodule deinitialised (empty dir) | `git ls-files -s sub` |
| `jj` | detached at `main` | `main`, `feature` | colocated jujutsu workspace: an empty `.jj/` beside `.git/` and a detached HEAD (T10.12) | `ls -d .jj; git symbolic-ref -q HEAD` (fails) |
| `lfs` | `main` | `main`, `feature` | Git LFS pointer files committed verbatim: `assets/hero.psd` changes on both sides, `data/table.dat` is added on `feature`, `notes.txt` is an ordinary file (T10.12) | `cat assets/hero.psd; git check-attr -a assets/hero.psd` |
| `crisscross` | `feature` | `main`, `feature` | two merge bases | `git merge-base --all main feature` |
| `rebase-detached` | detached (rebase) | `main`, `feature` | interrupted `git rebase -i` (`.git/rebase-merge/` present) | `git status` |
| `attributes` | `feature` | `main`, `feature` | `.gitattributes`: `-diff`, `binary`, `linguist-generated`, `text` | `git diff --numstat main...feature` |
| `sha256` | `main` | `main` | `--object-format=sha256` (refused by the engine) | `git rev-parse --show-object-format` |
| `alternates` | `feature` | as `basic` | `.git/objects/info/alternates` (refused) | `cat .git/objects/info/alternates` |
| `partial` | `main` | `main`, `origin/*` | `--filter=blob:none` promisor clone (refused) | `git config remote.origin.promisor` |
| `perf-5k` | `feature` | `main`, `feature`, `other` | 5,000 files / 200 dirs, packed; only with `FIXTURES_PERF=1` | `git ls-files \| wc -l` |
| `perf-log` | `main` | `main` + `topic01`…`topic50` | 2,000 linear commits, a commit-graph, packed refs; built by `git fast-import`, only with `FIXTURES_PERF=1` (T10.5b nit 8, `docs/perf.md`) | `git rev-list --count main` |

## v2 fixtures (T10.0, `docs/v2-contracts.md` § "Fixtures to add")

None of these has a `feature` branch, so the phase-3 parity suites that enumerate `fixtures/` skip them.

| Fixture | HEAD | Branches | What it exercises | Verify |
|---|---|---|---|---|
| `tags` | `main` | `main`, `release` | 2 lightweight + 2 annotated tags; `v1.0.0` sits on `release`'s tip; plus `blob-tag`/`tree-tag`, refs that do not name a commit (T10.5b B1) | `git tag -l --format='%(refname) %(objecttype) %(objectname) %(*objectname)'` |
| `stash` | `main` | `main` | two stashes, `stash@{0}` pushed with `-u` (three parents); worktree left dirty | `git stash list` |
| `rebase-conflict` | detached (rebase) | `main`, `topic` | `git rebase -i` stopped at step 2 of 3; `rebase-merge/{git-rebase-todo,done}` both present; one `UU`, one `AA` | `git status --porcelain=v1`; `cat .git/rebase-merge/msgnum .git/rebase-merge/end` |
| `merge-conflict` | `main` (merging) | `main`, `topic` | `MERGE_HEAD` present, one `UU`, one `UD` (modify/delete, deleted by them) | `git status --porcelain=v1` |
| `cherry-pick-conflict` | `main` (picking) | `main`, `topic` | `CHERRY_PICK_HEAD` present, one `UU` | `cat .git/CHERRY_PICK_HEAD` |
| `reflog-orphan` | `main` | `main` | `git reset --hard HEAD~2` left two commits reachable only from the reflog | `git reflog`; `git fsck --unreachable` |
| `history` | `main` | `main`, `topic`, `preflight/base`, `preflight/a` | 60 commits on `main` (one merge of `topic`, one root), `git mv` + edit rename, whitespace-only re-indent, `.mailmap` with two addresses for one author, a `renovate[bot]` author, 3 lightweight + 1 annotated tag, `commit-graph write --reachable --changed-paths`; `preflight/a` = one commit that collides with `main`, one touching the same file elsewhere, one clean | `git rev-list --count main`; `git log --graph --oneline --all` |
| `secrets` | `main` | `main` | fake AWS / GitHub / Anthropic keys and an RSA private key block across the staged and unstaged layers, a 40-char hex sha that must not be flagged, one `# diffgit:allow-secret` line | `git diff`; `git diff --cached` |
| `hidden` | `main` | `main` | ignored `dist/` (3 files), ignored `.env.local`, `skip-worktree` and `assume-unchanged` files edited on disk, an 11 MB tracked file modified in the worktree, an untracked `.DS_Store` (git shows it, diffgit's built-in excludes do not) | `git check-ignore -v <paths>`; `git ls-files -v` |
| `octopus` | `main` | `main`, `side-a`, `side-b` | three-parent merge commit; carries a commit-graph so its `EDGE` chunk is read (T10.5b nit 1) | `git rev-list --parents -n 1 HEAD` |
| `skew` | `main` | `main`, `side` | committer dates that do not decrease toward the parents: `P` is newer than its child `C`. Built with `git commit-tree` and explicit `GIT_COMMITTER_DATE` (T10.5b S1) | `git log --date-order --format=%H --all` |

`expected/` in every fixture is listed in that fixture's `.git/info/exclude` (written by the expectations script) so neither git nor the engine reports it as untracked. Alongside the JSON dumps, the v2 fixtures carry `expected/*.txt` — git's own output for `tag -l`, `stash list`, `reflog`, `ls-files -u`, `ls-files -v`, `check-ignore -v`, `log --graph`, `log -S`, `log --follow`, `blame --porcelain` (with and without `-w`), `rev-list --left-right --count`, `rev-list --bisect`, `rev-parse` and `shortlog -sn`. T10.5 added `log-head-date-order.txt` / `log-head-first-parent.txt` / `log-all-date-order.txt` (also for `crisscross`, `detached`, `skew`, `perf-5k` and `perf-log`), `commit-numstat.txt` (`git show --numstat --first-parent` per commit) and `branch-upstream.txt` / `branch-merged.txt` for the Branches table; T10.5b added `crisscross/expected/rev-list-left-right-count.txt`. `history/expected/preflight-outcome.txt` is the real `git rebase` result for `preflight/a` onto `main`, replayed on throwaway clones so the fixture itself is never rebased.
